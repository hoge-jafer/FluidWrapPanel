<UserControl
    x:Class="FluidWrapPanel.FluidItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluidWrapPanel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:controls="using:CompositionProToolkit.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="parent">

    <Interactivity:Interaction.Behaviors>
        <controls:FluidPointerDragBehavior DragButton="MouseLeftButton">
            
        </controls:FluidPointerDragBehavior>
    </Interactivity:Interaction.Behaviors>
    <Grid>
        <Border x:Name="fillboder"
                Margin="1"
                Background="{Binding Path=FillProperty,ElementName=parent}">
            <TextBlock x:Name="numText"
                       FontSize="18"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Binding Path=DataProperty,ElementName=parent}"/>
        </Border>
    </Grid>
</UserControl>
